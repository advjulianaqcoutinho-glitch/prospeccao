server {
    listen 80;
    server_name _;

    # Serve apenas arquivos estáticos — proxy é feito pelo Traefik
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
